# Dependencias de la interfaz web (FastAPI + Uvicorn).
# Se instalan ADEMAS de requirements.txt y agent/requirements-agent.txt.
#
#   pip install -r webapp/requirements-web.txt

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic[email]>=2.6.0
