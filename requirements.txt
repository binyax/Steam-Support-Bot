# Dependencias del Steam-Support-Bot (notebooks 01-08).
# El módulo del agente autónomo tiene dependencias adicionales en:
#   agent/requirements-agent.txt  (crewai, faiss-cpu, ...)

# --- Núcleo: conexión con GPT-4o vía GitHub Models y framework LangChain ---
langchain>=0.3.0
langchain-openai>=0.3.0
langchain-community>=0.3.0          # vectorstores (FAISS) y loaders (TextLoader)
langchain-text-splitters>=0.3.0     # fragmentación de texto para RAG
openai>=1.0.0                       # cliente OpenAI usado directamente (notebooks 01 y 02)

# --- RAG: base de datos vectorial local ---
faiss-cpu>=1.8.0

# --- Manejo seguro de credenciales (.env) ---
python-dotenv>=1.0.0

# --- Observabilidad: trazas de las respuestas de la IA ---
langsmith>=0.1.0

# --- Utilidades ---
pypdf>=4.0.0          # lectura de PDFs (p. ej. reglas de Steam)
tiktoken>=0.8.0       # conteo de tokens para no exceder el límite
pandas>=2.0.0         # reportes tabulares en la evaluación RAG (notebook 07)
ipykernel>=6.0.0      # ejecución de los notebooks .ipynb en VS Code
requests>=2.31.0      # consultas HTTP a recursos externos de Steam
